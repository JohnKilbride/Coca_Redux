import numpy as np
import raster

if __name__ == "__main__":
    
    # read in the raster array
    raster_array = np.random.rand(8,1000,1000)
    
    # Spatially index the raster layer adn apoply the model to each subset
    
    # Modify the spatial extent of the output raster dataset
    
    #
    